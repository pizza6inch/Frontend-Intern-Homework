function toTokenAuthentication({ // Defines the function "toTokenAuthentication"
  installationId,
  token,
  createdAt,
  expiresAt,
  repositorySelection,
  permissions,
  repositoryIds, // Input property for repository IDs
  repositoryNames, // Input property for repository names
  singleFileName // Input property for single file name
}) {
  return Object.assign(
    { // Begins creation of new object
      type: "token", // Sets the type to "token"
      tokenType: "installation", // Sets the token type to "installation"
      token, // Sets the token to the input token
      installationId, // Sets the installation ID to the input installation ID
      permissions, // Sets the permissions to the input permissions
      createdAt, // Sets the created at to the input createdAt
      expiresAt, // Sets the expires at to the input expiresAt
      repositorySelection // Sets the repository selection to the input repositorySelection
    },
    repositoryIds ? { repositoryIds } : null, // If repositoryIds is present, include it in the new object
    repositoryNames ? { repositoryNames } : null, // If repositoryNames is present, include it in the new object
    singleFileName ? { singleFileName } : null // If singleFileName is present, include it in the new object
  );
}
export { // Exports the function
  toTokenAuthentication
};
