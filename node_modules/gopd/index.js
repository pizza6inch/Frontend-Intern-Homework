var getOwnPropertyDescriptor = module.exports;

if (getOwnPropertyDescriptor) {
  var desc = getOwnPropertyDescriptor(myObject, 'myProperty');
  // desc is an object with properties 'value', 'writable', 'enumerable', and 'configurable'
} else {
  // Handle the case where Object.getOwnPropertyDescriptor is not available
}
