/**
 * The `RequestMethod` type represents the HTTP verb supported by GitHub's REST API.
 * By defining this type, we can ensure that only valid HTTP methods are used when making requests to the GitHub API.
 * The possible values for this type are:
 * - "DELETE": Used to delete a resource.
 * - "GET": Used to retrieve a resource.
 * - "HEAD": Used to retrieve header information about a resource without actually getting the resource itself.
 * - "PATCH": Used to make partial updates to a resource.
 * - "POST": Used to create a new resource.
 * - "PUT": Used to update an existing resource.
 */
export type RequestMethod = "DELETE" | "GET" | "HEAD" | "PATCH" | "POST" | "PUT";
