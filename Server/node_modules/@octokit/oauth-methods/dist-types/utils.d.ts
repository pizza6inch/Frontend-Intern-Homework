import type { RequestInterface } from "@octokit/types";

// This function takes in a RequestInterface object as an argument and returns a string
// which is the base URL for OAuth requests. It extracts the necessary information from
// the RequestInterface object to construct the URL.
export declare function requestToOAuthBaseUrl(request: RequestInterface): string;

// This function takes in a RequestInterface object, a route string, and a parameters
// object as arguments. It sends an OAuth request using the provided parameters and
// returns a Promise that resolves to an OctokitResponse object containing the response
// data from the OAuth request.
export declare function oauthRequest(request: RequestInterface, route: string, parameters: Record<string, unknown>): Promise<import("@octokit/types").OctokitResponse<any>>;

