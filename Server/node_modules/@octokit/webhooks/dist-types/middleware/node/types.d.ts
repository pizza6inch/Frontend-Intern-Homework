import type { Logger } from "../../createLogger.js";

// Defines an options type for middleware with two optional properties:
// - path (string): A specific path to apply the middleware to.
// - log (Logger): A logger instance to use for logging.
export type MiddlewareOptions = {
    path?: string;
    log?: Logger;
};
