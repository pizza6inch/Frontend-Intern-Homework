const coverageReporter = require('lcov-reporter');
const config = {
  all: true,
  check-coverage: false,
  reporter: ['text-summary', 'text', 'html', 'json'],
  exclude: [
    'coverage',
    'test'
  ]
};

coverageReporter.create(config);
