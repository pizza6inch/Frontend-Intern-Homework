// Import the 'semver' module for working with semantic versioning
const semver = require('semver');

// Export the result of the 'satisfies' method, which checks if the current
// Node.js version (accessed via 'process.version') meets the specified
// version range ('>=16.9.0')
module.exports = semver.satisfies(process.version, '>=16.9.0');

