/**
 * @module "constants"
 * @description This module is a declaration file that exports a single const object containing various constants from different Node.js modules. It is marked as deprecated since v6.3.0, and it is recommended to use the constants property exposed by the relevant module instead.
 * @deprecated since v6.3.0 - use constants property exposed by the relevant module instead.
 */

/**
 * @module "node:constants"
 * @description This module is a re-export of the "constants" module. It imports the "constants" module and re-exports it as is.
 */

declare module "constants" {
    // Import specific constant sets from Node.js modules
    import { errno as osErrno, priority as osPriority } from "node:os";
    import { SignalConstants } from "node:os";
    import { constants as cryptoConstants } from "node:crypto";
    import { constants as fsConstants } from "node:fs";

    /**
     * @const
     * @description An object that combines various constants from Node.js modules: errno and priority from the 'os' module, SignalConstants from the 'os' module, constants from the 'crypto' module, and constants from the 'fs' module.
     */
    const exp: {
        // Combine errno and priority constants from the 'os' module
        readonly errno: typeof osErrno;
        readonly priority: typeof osPriority;
        // Import SignalConstants from the 'os' module
        readonly SIGTERM: SignalConstants.SIGTERM;
        readonly SIGINT: SignalConstants.SIGINT;
        readonly SIGUSR1: SignalConstants.SIGUSR1;
        readonly SIGUSR2: SignalConstants.SIGUSR2;
        // Import constants from the 'crypto' module
        readonly RSA_PKCS1_OAEP_PADDING: cryptoConstants.RSA_PKCS1_OAEP_PADDING;
        readonly RSA_PKCS1_PADDING: cryptoConstants.RSA_PKCS1_PADDING;
        // Import constants from the 'fs' module

