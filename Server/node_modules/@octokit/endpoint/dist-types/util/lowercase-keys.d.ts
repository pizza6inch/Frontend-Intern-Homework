const input = {
  "UpperCase": "value",
  "mixedCase": "another value"
};

const output = lowercaseKeys(input);

console.log(output); // { "uppercase": "value", "mixedcase": "another value" }
console.log(input); // { "UpperCase": "value", "mixedCase": "another value" }
