import type { OAuthAppResult, GitHubAppResult } from "@octokit/oauth-authorization-url";
import type { RequestInterface } from "@octokit/types";

// Define the options for getting the web flow authorization URL for OAuth App
export type GetWebFlowAuthorizationUrlOAuthAppOptions = {
    // The type of client, which is an OAuth App
    clientType: "oauth-app";
    // The client ID for the OAuth App
    clientId: string;
    // Optionally allow signup for the user
    allowSignup?: boolean;
    // Optionally specify the GitHub username for the login
    login?: string;
    // Optionally specify the scopes for the OAuth App
    scopes?: string | string[];
    // Optionally specify the redirect URL for the OAuth App
    redirectUrl?: string;
    // Optionally specify the state for the OAuth App
    state?: string;
    // Optionally specify the request interface for the OAuth App
    request?: RequestInterface;
};

// Define the options for getting the web flow authorization URL for GitHub App
export type GetWebFlowAuthorizationUrlGitHubAppOptions = {
    // The type of client, which is a GitHub App
    clientType: "github-app";
    // The client ID for the GitHub App
    clientId: string;
    // Optionally allow signup for the user
    allowSignup?: boolean;
    // Optionally specify the GitHub username for the login
    login?: string;
    // Optionally specify the redirect URL for the GitHub App
    redirectUrl?: string;
    // Optionally specify the state for the GitHub App
    state?: string;
    // Optionally specify the request interface for the GitHub App
    request?: RequestInterface;
};

// Define the result types for the web flow authorization URL
export type GetWebFlowAuthorizationUrlOAuthAppResult = OAuthAppResult;
export type GetWebFlowAuthorizationUrlGitHubAppResult = GitHubAppResult;

// Declare the function for getting the web flow authorization URL for OAuth App
export declare function getWebFlow
