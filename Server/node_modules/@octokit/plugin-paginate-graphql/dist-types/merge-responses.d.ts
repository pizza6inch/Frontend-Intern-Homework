// Declare a constant named 'mergeResponses' which is a function that accepts two arguments:
// 'response1' and 'response2' of the same type 'ResponseType_1' (defaulted to 'any' if not provided).
// The function returns a value of the same type 'ResponseType_1'.
declare const mergeResponses: <ResponseType_1 extends object = any>(
  response1: ResponseType_1,
  response2: ResponseType_1
) => ResponseType_1;

// Export the 'mergeResponses' constant so it can be used in other modules.
export { mergeResponses };
