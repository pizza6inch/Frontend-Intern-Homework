// Import the 'Authentication' type from './types' file.
import type { Authentication } from "./types";

// Declare an exported function named 'auth' which takes in a string 'reason' as an argument.
// This function returns a Promise that resolves to an 'Authentication' object.
export declare function auth(reason: string): Promise<Authentication>;

