// Import the package.json file from the parent directory
const packagejson = require('../package.json');

// Log the version number of the current project to the console
// The version number is obtained from the package.json file
console.log(JSON.stringify({ version: packagejson.version }));

