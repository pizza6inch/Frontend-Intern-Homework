/**
 * Helpers.
 */
// Constants representing time units in milliseconds
var s = 1000; // seconds
var m = s * 60; // minutes
var h = m * 60; // hours
var d = h * 24; // days
var w = d * 7; // weeks
var y = d * 365.25; // years

/**
 * Parse or format the given `val`.
 *
 * Options:
 *
 *  - `long` verbose formatting [false]
 *
 * @param {String|Number} val
 * @param {Object} [options]
 * @throws {Error} throw an error if val is not a non-empty string or a number
 * @return {String|Number}
 * @api public
 */
// Main function that takes a string or number input and returns a formatted string or number representing a duration in time

/**
 * Parse the given `str` and return milliseconds.
 *
 * @param {String} str
 * @return {Number}
 * @api private
 */
// Helper function to parse a string input and return the equivalent number of milliseconds

/**
 * Short format for `ms`.
 *
 * @param {Number} ms
 * @return {String}
 * @api private
 */
// Helper function to format a number of milliseconds into a short string representation

/**
 * Long format for `ms`.
 *
 * @param {Number} ms
 * @return {String}
 * @api private
 */
// Helper function to format a number of milliseconds into a long string representation

/**
 * Pluralization helper.
 */
// Helper function to handle pluralization of time units
