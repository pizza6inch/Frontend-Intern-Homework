import { Octokit } from "@octokit/core";
import type { RequestError } from "@octokit/request-error";

// The current version of the package, which is "0.0.0-development".
export declare const VERSION = "0.0.0-development";

/**
 * A function to retry Octokit requests with exponential backoff.
 * @param octokit - The Octokit instance to use for making requests.
 * @param octokitOptions - Options to configure the Octokit instance.
 * @returns An object with a `retryRequest` method that can be used to retry failed requests.
 */
export declare function retry(octokit: Octokit, octokitOptions: any): {
    retry: {
        // The function to use for retrying failed requests.
        retryRequest: (error: RequestError, retries: number, retryAfter: number) => RequestError;
    };
};

// The `retry` namespace, which contains the `VERSION` constant.
export declare namespace retry {
    // The version of the `retry` namespace, which is the same as the package version.
    var VERSION: string;
}
