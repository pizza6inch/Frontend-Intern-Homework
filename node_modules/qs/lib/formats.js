'use strict';

// Create a regular expression to match %20, which represents a space in a URL
var percentTwenties = /%20/g;

// Define an object for URL encoding formats
var Format = {
    RFC1738: 'RFC1738', // Specifies the RFC1738 format
    RFC3986: 'RFC398
