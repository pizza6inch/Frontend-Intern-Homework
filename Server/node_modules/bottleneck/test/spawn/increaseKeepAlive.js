// Import the Bottleneck library, which provides rate limiting functionality
var Bottleneck = require('../bottleneck.js')

// Get the current timestamp in milliseconds
var now = Date.now()

// Create a new rate limiter using the Bottleneck library
// The limiter is configured with the following options:
// - reservoir: 2 - The initial number of permits in the reservoir
// - reservoirIncreaseAmount: 2 - The number of permits to add to the reservoir
//   every reservoirIncreaseInterval milliseconds
// - reservoirIncreaseInterval: 200 - The time interval in milliseconds to
//   increase the reservoir by reservoirIncreaseAmount permits
var limiter = new Bottleneck({
  reservoir: 2,
  reservoirIncreaseAmount: 2,

