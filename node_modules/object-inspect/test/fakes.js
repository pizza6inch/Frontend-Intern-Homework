'use strict';

// Import the 'inspect' function from the relative path '../'
var inspect = require('../');

// Import the 'tape' module for testing
var test = require('tape');

// Import the 'has-tostringtag' module to check if the global object has the 'Symbol.toStringTag' property
var hasToStringTag = require('has-tostringtag/shams')();

// Import the 'for-each' function to iterate over arrays
var forEach = require('for-each');

// Start the test suite with the 'test' function, passing an object with the 'skip' property set to the inverse of 'hasToStringTag'
test('fakes', { skip: !hasToStringTag }, function (t) {
    // Initialize an array of expected string values for the 'Symbol.toStringTag' property
    var expectedValues = [
        'Array',
        'Boolean',
        'Date',
        'Error',
        'Number',

