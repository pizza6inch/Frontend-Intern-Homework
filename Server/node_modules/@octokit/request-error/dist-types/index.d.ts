import type { RequestOptions, ResponseHeaders, OctokitResponse } from "@octokit/types";
import type { RequestErrorOptions } from "./types";

/**
 * Export the RequestError class which extends the built-in Error class.
 * This class is used to represent an error that occurred during an HTTP request.
 */
export declare class RequestError extends Error {
    // Define the name property as "HttpError" to indicate the type of error.
    name: "HttpError";

    /**

