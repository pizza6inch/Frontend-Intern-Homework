'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

// Define a Deprecation class that extends the built-in Error class
class Deprecation extends Error {
  // Constructor for Deprecation class
  constructor(message) {
    // Call the parent constructor with the provided message
    super(message); // Maintains proper stack trace (only available on V8)

    /* istanbul ignore next */

    // If Error.captureStackTrace is available, use it to capture a stack trace
    if (Error.captureStackTrace) {
      Error.captureStackTrace(this, this.constructor);
    }

    // Set the name property of the instance to 'Deprecation'
    this.name = 'Deprecation';
  }

}

// Export the Deprecation class as a named export
exports.Deprecation = Deprecation;

