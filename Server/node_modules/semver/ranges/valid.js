const validRange = require('./validRange')

const range = '1-10'
const options = {step: 2}

const validatedRange = validRange(range, options)

if (validatedRange) {
  console.log(`The range ${range} is valid.`)
} else {
  console.log(`The range ${range} is invalid.`)
}


The range 1-10 is valid.


The range 1-10 is invalid.
