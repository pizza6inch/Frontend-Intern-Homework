declare namespace _exports {
    // The `_exports` namespace is used to export the `Func` type and the
    // default export function.
    export { Func };
}

declare const _exports: <T extends Func = Func>(fn: T, length: number, loose?: boolean) => T;
// The `_exports` constant is a generic function that takes up to three arguments:
// - `fn`: A function to be processed, with a default type of `Func`.
// - `length`: The expected length of the `fn` argument's argument list.
// - `loose` (optional): A boolean flag to enable loose mode, which defaults to `false`.
// The function returns a copy of the input function with additional type constraints.

export = _exports;
// The `export` statement makes the `_exports` constant available for other modules to import.

type Func = (...args: unknown[]) => unknown;
// The `Func` type is an alias for a function that accepts any number of arguments
// and returns an `unknown` type.

//# sourceMappingURL=index.d.ts.map
// This line is a reference to a source map file, which helps debugging tools map
// between the compiled and original source code.
