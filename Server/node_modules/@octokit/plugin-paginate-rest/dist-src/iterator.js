import { Octokit } from "@octokit/rest";
import { iterator } from "./iterator.js";

const octokit = new Octokit();
const repoIterator = iterator(octokit, "repos.listForOrg", { org: "octokit" });

(async () => {
  for await (const
