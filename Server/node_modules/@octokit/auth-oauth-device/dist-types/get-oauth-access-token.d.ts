const state = {
  // app state information
};

const auth = {
  // authentication information
};

getOAuthAccessToken(state, { auth })
  .then(authInfo => {
    // use the access token from authInfo
  })
  .catch(error => {
    // handle any errors
  });

