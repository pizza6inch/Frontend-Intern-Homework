import { toTokenAuthentication } from "./myModule";
import type { CacheData, InstallationAccessTokenAuthentication } from "./types";

const cacheData: CacheData & WithInstallationId = {
  installationId: 1234,
  token: "my-token",
  createdAt: new Date(),
  expiresAt: new Date(),
  repositorySelection: "selected",
  permissions: "read-only",
  repositoryIds: [1, 2, 3],
  repositoryNames: ["repo1", "repo2", "repo3"],
  singleFileName: "my-file.txt",
};

const authentication: InstallationAccessTokenAuthentication = toTokenAuthentication(cacheData);
