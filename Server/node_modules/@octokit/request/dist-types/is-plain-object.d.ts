declare const value: unknown;

if (isPlainObject(value)) {
  // At this point, TypeScript knows that `value` is a plain object,
  // so you can use object-specific methods like `hasOwnProperty`.
  console.log(value.hasOwnProperty('foo'));
}
