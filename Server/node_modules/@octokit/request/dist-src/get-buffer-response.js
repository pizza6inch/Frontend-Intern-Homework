// Function: getBufferResponse(response)
// 
// This function is used to extract an ArrayBuffer from a response object.
// It takes a single argument, 'response', which is expected to be an instance
// of the Fetch API's Response object.
// 
// Returns: Promise<ArrayBuffer>
// 
// The function returns a Promise that resolves to an ArrayBuffer containing
// the response data.
function getBufferResponse(response) {
  return response.arrayBuffer();
}

// Exporting the default export as 'getBufferResponse'
export {
  getBufferResponse as default
};

