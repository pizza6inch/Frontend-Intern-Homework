'use strict';

/**
 * Exports a ReferenceError object, which is a built-in JavaScript error that
 * indicates that an attempt to reference a variable or property that does not
 * exist has occurred. This error is often thrown when a variable is not
 * declared before it is used.
 *
 * The expected type of the module that is being exported is specified using
 * the `@type` comment at the beginning of the code. In this case, the expected
 * type is a ReferenceError object imported from a module named 'ref'.
 *
 * @type {import('./ref')}
 * @module
 */
module.exports = ReferenceError;

