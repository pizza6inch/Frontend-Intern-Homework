// Importing Handler from '../../handler' which is likely a custom Lambda handler interface or class.
import { Handler } from "../../handler";

// Importing BaseTriggerEvent and StringMap from './_common' which are likely common types or interfaces used in the application.
import { BaseTriggerEvent, StringMap } from "./_common";

// Defining an interface 'BaseCustomSMSSenderTriggerEvent' which extends 'BaseTriggerEvent' and adds a 'request' property.
// The 'request' property is an object that contains 'type', 'code', 'userAttributes', and 'clientMetadata' properties.
export interface BaseCustomSMSSenderTriggerEvent<T extends string> extends BaseTriggerEvent<T> {
    request: {
        type: string; // The type of request.
        code: string | null; // The verification code.
        userAttributes: StringMap; // A map of user attributes.
        clientMetadata?: StringMap | undefined; // Optional client metadata.
    };
}

