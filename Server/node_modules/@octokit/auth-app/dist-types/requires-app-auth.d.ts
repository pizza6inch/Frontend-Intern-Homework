const isAuthenticated = requiresAppAuth('https://example.com/private-area');
if (isAuthenticated) {
  // The user is authenticated, so show them the private content.
} else {
  // The user is not authenticated, so redirect them to the login page.
}
