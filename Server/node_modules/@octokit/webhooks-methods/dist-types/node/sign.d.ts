// Import statement to bring in the 'SignOptions' type from '../types.js' file.
import { type SignOptions } from "../types.js";

// Function export declaration for 'sign' which takes two parameters:
// - 'options': An instance of 'SignOptions' type or a string.
// - 'payload': A string to be signed.
// Returns a Promise that resolves to a signed string.
export declare function sign(options: SignOptions | string, payload: string): Promise<string>;

// Namespace export declaration for 'sign' with a 'VERSION' property.
export declare namespace sign {
    // 'VERSION' property is a string that represents the version of the 'sign' function.
    var VERSION: string;
}
