const URIError = require('./uri-error');

try {
  const url = new URL('ht//tp:/example.com');
} catch (e) {
  if (e instanceof URIError) {
    console.error(`A URI error occurred: ${e.message}`);
  } else {
    throw e;
  }
}
