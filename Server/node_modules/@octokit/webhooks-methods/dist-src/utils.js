// This function, getAlgorithm, takes a signature as an argument and returns
// the corresponding algorithm used to generate the signature.
// It checks if the signature starts with "sha256=" and returns "sha256" if true,
// otherwise it returns "sha1".
const getAlgorithm = (signature) => {
  return signature.startsWith("sha256=") ? "sha256" : "sha1";
};

// The export statement makes the getAlgorithm function available for import
// in other modules.
export {
  getAlgorithm
};

