try {
  // Import the 'util' module.
  var util = require('util');

  /* istanbul ignore next */
  // Check if the 'util.inherits' function is available. If not, throw an empty error.
  if (typeof util.inherits !== 'function') {
    throw '';
  }

  // If 'util.inherits' is available, export it as the module.
  module.exports = util.inherits;

} catch (e) {

  /* istanbul ignore next */
  // If 'util.inherits' is not available, import the browser-compatible version.
  module.exports = require('./inherits_browser.js');

}
