import { auth, OAuthAppAuthOptions, GitHubAppAuthOptions } from "./myAuthModule";

const oauthAppAuthOptions: OAuthAppAuthOptions = {
  // configuration for OAuth app authentication
};

const githubAppAuthOptions: GitHubAppAuthOptions = {
  // configuration for GitHub app authentication
};

const oauthAppState: OAuthAppState = {
  // OAuth app state
};

const githubAppState: GitHubAppState = {
  // GitHub app state
};

auth(oauthAppState, oauthAppAuthOptions)
  .then((oauthAppAuth) => {
    // handle OAuth app authentication
  })
  .catch((error) => {
    // handle error during OAuth app authentication
  });

auth(githubAppState, githubAppAuthOptions)
  .then((githubAppAuth) => {
    // handle GitHub app authentication
  })
  .catch((error) => {
    // handle error during GitHub app authentication
  });
