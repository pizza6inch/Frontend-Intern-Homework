'use strict';

// Import the 'tape' module for testing
var test = require('tape');

// Check if the environment has native Symbol support
if (typeof Symbol === 'function' && typeof Symbol() === 'symbol') {
  // Define a test that checks if the 'Symbol' function is a function and if it returns a symbol when called
  test('has native Symbol support', function (t) {
    t.equal(typeof Symbol, 'function');
    t.equal(typeof Symbol(), 'symbol');
    t.end();
  });
  return;
}

// Import the 'shams' module to check for native Symbol support
var hasSymbols = require('../../shams');

// Define a test that checks if Symbol is polyfilled
test('polyfilled Symbols', function (t) {
  /* eslint-disable global-require */
  // Check the value of 'hasSymbols()' before polyfilling Symbol
  t.equal(hasSymbols(), false, 'hasSymbols is false before polyfilling');

  // Polyfill Symbol and its 'to-string-tag'
  require('core-js/fn/symbol');
  require('core
