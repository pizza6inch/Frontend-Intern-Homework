// This code exports the 'strict' assertion module from Node.js,
// which provides a more strict version of the built-in 'assert' module.
// This module is useful for writing more robust tests and ensuring
// that values meet specific conditions.

// The first declare module statement exports the 'strict' module
// from the 'assert' package in Node.js. This allows other modules
// to import and use the 'strict' module as if it were a local module.
declare module "assert/strict" {
    import { strict } from "node:assert";
    export = strict;
}

// The second declare module statement is redundant, as it exports
// the same 'strict' module from the 'assert' package in Node.js.
// It is identical to the first declare module statement and does not
// provide any additional functionality.
declare module "node:assert/strict" {
    import { strict } from "node:assert";
    export = strict;
}
