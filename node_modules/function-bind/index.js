'use strict';

// Import the 'implementation' module for use in the event that the 'bind' method
// is not supported natively by the Function object.
var implementation = require('./implementation');

// Export a function that acts as the 'bind' method for the Function object,
// either the native 'bind' method or the one provided by the 'implementation'
// module. This is done to ensure compatibility across different JavaScript
// environments.
module.exports = Function.prototype.bind || implementation;

